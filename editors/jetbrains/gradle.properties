# Target IDE for building/running the plugin. WebStorm ('WS') and PhpStorm
# ('PS') both ship the LSP API; pick the one (and version) you target.
platformType = WS
platformVersion = 2026.1.3
pluginSinceBuild = 261
kotlin.stdlib.default.dependency = false
org.gradle.jvmargs = -Xmx2g
