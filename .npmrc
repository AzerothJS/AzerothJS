# Enforce the "engines" floor (Node >= 22) at install time instead of only
# warning, so a contributor on an unsupported Node fails fast with a clear
# message rather than hitting subtle runtime errors later.
engine-strict=true
