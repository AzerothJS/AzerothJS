// Fixture: keeps this app's tsconfig from having zero `.ts` inputs; the
// deliberate type error lives in the sibling `.azeroth` file only.
export const REASON: string = 'the .azeroth file carries the only type error';
