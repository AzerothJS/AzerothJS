## Router

This app was scaffolded with `--router`: the framework's own client-side router,
no extra dependency. `src/routes.ts` is the one route table, `src/pages/` holds
one component per route, and `src/App.azeroth` is the shell (`<Link>` nav plus
the `<Routes>` outlet). Adding a page is one row plus its component.
