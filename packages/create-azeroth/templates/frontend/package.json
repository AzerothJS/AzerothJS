{
    "name": "{{name}}",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "engines": {
        "node": ">=24"
    },
    "scripts": {
        "dev": "azeroth dev",
        "check": "azeroth check",
        "build": "azeroth build",
        "test": "vitest run",
        "preview": "vite preview"
    },
    "dependencies": {
        "azerothjs": "{{version}}"
    },
    "devDependencies": {
        "@azerothjs/cli": "{{version}}",
        "@azerothjs/compiler": "{{version}}",
        "@azerothjs/eslint-plugin": "{{version}}",
        "@azerothjs/language-server": "{{version}}",
        "@azerothjs/testing": "{{version}}",
        "@azerothjs/typescript-plugin": "{{version}}",
        "@eslint/js": "^10.0.1",
        "eslint": "^10.7.0",
        "globals": "^17.7.0",
        "happy-dom": "^20.0.0",
        "typescript": "~6.0.2",
        "typescript-eslint": "^8.63.0",
        "vite": "^8.1.4",
        "vitest": "^4.0.0"
    }
}
