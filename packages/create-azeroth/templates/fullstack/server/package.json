{
    "name": "{{name}}-server",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "engines": {
        "node": ">=24"
    },
    "scripts": {
        "dev": "azeroth dev",
        "check": "azeroth check",
        "test": "vitest run",
        "start": "node src/main.ts"
    },
    "dependencies": {
        "@azerothjs/http": "{{version}}",
        "@azerothjs/kit": "{{version}}",
        "@azerothjs/logger": "{{version}}",
        "@azerothjs/schema": "{{version}}",
        "azerothjs": "{{version}}"
    },
    "devDependencies": {
        "@azerothjs/cli": "{{version}}",
        "@azerothjs/devtools": "{{version}}",
        "@azerothjs/eslint-plugin": "{{version}}",
        "@azerothjs/ws": "{{version}}",
        "@eslint/js": "^10.0.1",
        "@types/node": "^26.1.0",
        "eslint": "^10.7.0",
        "globals": "^17.7.0",
        "jiti": "^2.7.0",
        "typescript": "^6.0.3",
        "typescript-eslint": "^8.64.0",
        "vitest": "^4.1.9"
    }
}
