{
    "name": "{{name}}-server",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "engines": {
        "node": ">=22"
    },
    "scripts": {
        "test": "vitest run",
        "start": "node src/main.ts"
    },
    "dependencies": {
        "@azerothjs/http": "{{version}}",
        "@azerothjs/kit": "{{version}}",
        "@azerothjs/logger": "{{version}}",
        "@azerothjs/schema": "{{version}}"
    },
    "devDependencies": {
        "@types/node": "^24.0.0",
        "typescript": "~6.0.2",
        "vitest": "^4.0.0"
    }
}
