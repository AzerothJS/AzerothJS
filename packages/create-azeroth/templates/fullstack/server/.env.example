# Copy to .env for local overrides (never commit .env). Read by src/config.ts.
PORT=3000
NODE_ENV=development
# Where the built client lives in production (the server serves it - one origin).
CLIENT_DIR=../application/dist
