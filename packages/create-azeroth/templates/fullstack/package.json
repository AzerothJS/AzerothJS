{
    "name": "{{name}}",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "engines": {
        "node": ">=24"
    },
    "workspaces": [
        "application",
        "server"
    ],
    "scripts": {
        "dev": "azeroth dev",
        "check": "azeroth check",
        "build": "azeroth build",
        "test": "npm test --workspaces --if-present",
        "start": "node server/src/main.ts"
    },
    "devDependencies": {
        "@azerothjs/cli": "{{version}}"
    }
}
