{
    "name": "{{name}}",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "workspaces": [
        "application",
        "server"
    ],
    "scripts": {
        "dev": "azeroth dev",
        "check": "azeroth check",
        "build": "azeroth build"
    },
    "devDependencies": {
        "@azerothjs/cli": "{{version}}"
    }
}
