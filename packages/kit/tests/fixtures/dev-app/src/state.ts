// What the page action writes and what the revalidating page counts - both read back through a
// render, so an arm sees the module vite loaded rather than a copy of it.
export const signed: string[] = [];

export const readings = { fresh: 0 };
